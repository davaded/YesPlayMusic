<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@drawable/bg_luminous_dark">

  <!-- 环境光层 -->
  <View
    android:layout_width="500dp"
    android:layout_height="500dp"
    android:layout_gravity="start|top"
    android:layout_marginStart="-200dp"
    android:layout_marginTop="-150dp"
    android:background="@drawable/bg_ambient_orb_blue"
    android:alpha="0.15" />

  <View
    android:layout_width="400dp"
    android:layout_height="400dp"
    android:layout_gravity="end|bottom"
    android:layout_marginEnd="-150dp"
    android:layout_marginBottom="-100dp"
    android:background="@drawable/bg_ambient_orb_pink"
    android:alpha="0.1" />

  <androidx.core.widget.NestedScrollView
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:overScrollMode="never"
    android:fillViewport="true">

    <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:orientation="vertical"
      android:paddingStart="@dimen/shell_padding"
      android:paddingEnd="@dimen/shell_padding"
      android:paddingTop="32dp"
      android:paddingBottom="@dimen/shell_padding">

      <!-- 页面标题 -->
      <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:text="@string/tab_recommend"
        android:textColor="@color/textPrimary"
        android:textSize="28sp"
        android:fontFamily="@font/barlow_bold"
        android:letterSpacing="-0.03" />

      <!-- 分类标签 -->
      <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:scrollbars="none"
        android:overScrollMode="never">

        <LinearLayout
          android:id="@+id/tagsContainer"
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:orientation="horizontal" />

      </HorizontalScrollView>

      <!-- 精品歌单区域 -->
      <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="@string/section_highquality"
        android:textColor="@color/textPrimary"
        android:textSize="20sp"
        android:fontFamily="@font/barlow_semibold"
        android:letterSpacing="-0.02" />

      <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/highqualityGrid"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="32dp"
        android:clipToPadding="false"
        android:overScrollMode="never" />

      <!-- 热门歌单区域 -->
      <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="@string/section_hot_playlists"
        android:textColor="@color/textPrimary"
        android:textSize="20sp"
        android:fontFamily="@font/barlow_semibold"
        android:letterSpacing="-0.02" />

      <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/hotGrid"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:overScrollMode="never" />

    </LinearLayout>
  </androidx.core.widget.NestedScrollView>

  <!-- 加载指示器 -->
  <ProgressBar
    android:id="@+id/loadingIndicator"
    android:layout_width="48dp"
    android:layout_height="48dp"
    android:layout_gravity="center"
    android:indeterminate="true"
    android:visibility="gone" />

</FrameLayout>
